import { createContext } from 'react';

export const BlockContext = createContext( 'block' );
export const DataContext = createContext( 'data' );
export const InputContext = createContext( 'input' );
